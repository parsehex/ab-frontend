{
  "name": "airmash-frontend",
  "version": "2.0.8",
  "description": "",
  "private": true,
  "scripts": {
    "build": "node src/update-version && node src/update-games-data && patch-package && vite build && node src/build-style.js",
    "watch": "node src/update-version && node src/update-games-data && patch-package && node src/build-style.js && vite build --watch --emptyOutDir=false",
    "start": "http-server dist -p 8000",
    "postinstall": "patch-package"
  },
  "devDependencies": {
    "howler": "2.0.5",
    "jquery": "3.5.0",
    "js-cookie": "2.2.0",
    "nipplejs": "0.6.7",
    "patch-package": "^6.5.1",
    "path-browserify": "^1.0.1",
    "perfect-scrollbar": "0.8.0",
    "pixi.js": "4.5.6",
    "url": "^0.11.4",
    "vite": "^8.0.0",
    "vite-plugin-static-copy": "^3.1.4"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
